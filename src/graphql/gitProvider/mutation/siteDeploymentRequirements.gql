mutation siteDeploymentRequirements(
  $where: SiteDeploymentRequirementsWhereInput!
) {
  siteDeploymentRequirements(where: $where) {
    authorizationUrl
    installationUrl
    shouldAuthenticate
    shouldInstall
  }
}
