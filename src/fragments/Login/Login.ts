import { LoginLayout } from './Layout';
import { LoginStyles } from './Login.styles';
import { LoginDialog } from './LoginDialog';

export const Login = {
  Layout: LoginLayout,

  Dialog: LoginDialog,
  Spinner: LoginStyles.Spinner,
};
