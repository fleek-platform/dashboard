import { styled } from '@/theme';
import { Box } from '@/ui';

export const GitIntegrationStyles = {
  Alert: {
    Wrapper: styled(Box, {
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'space-between',
    }),
  },
};
