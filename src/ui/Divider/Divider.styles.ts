import { styled } from '@/theme';

export const DividerElement = styled('hr', {
  width: '$full',
  border: 'none',
  minHeight: '$borderWidths$base',
  backgroundColor: '$border-slate',
  margin: 0,
});
