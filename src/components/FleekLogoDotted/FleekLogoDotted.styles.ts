import { styled } from '@/theme';

export const FleekLogoDottedStyles = {
  Video: styled('video', {
    height: '60vh',
    maxWidth: '$full',
  }),
  Custom: styled('svg', {
    width: '11.5625rem',
  }),
};
